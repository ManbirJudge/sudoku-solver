# Sudoku Solver Backend
- `solver.py` contains the original solving algorithm.
- `train_digit_model.py` trains the digit recognition model.
- `paper_finder.py` containers the original CV stuff.
## TODOs
- Check if `paper_finder.py` works. After that, keep it if it's useful, else delete it.
- Verify that the current training dataset is the original one.
    - From output model, it seems like it.
    - Accuracy of 85% is low BUT, it wasn't that amazing previously too.